[project]
name = "enterprise-iam"
version = "0.1.0"
description = "Enterprise Identity and Access Management (IAM) service built with FastAPI."
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "fastapi>=0.139.0",
    "requests>=2.34.2",
    "uvicorn[standard]>=0.51.0",
]
